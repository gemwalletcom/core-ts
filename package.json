{
  "name": "swap-providers-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "jest",
    "dev": "npm run dev --workspace @swap-providers/api"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/jest": "29.5.14",
    "@types/node": "22.13.14",
    "jest": "29.7.0",
    "nodemon": "3.1.0",
    "ts-jest": "29.3.1",
    "ts-node": "10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "5.8.2"
  }
}
